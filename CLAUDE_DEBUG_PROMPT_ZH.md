# Claude 默认背景提示词（低 Token + 易排查版）

> 目标：平时少消耗 token；bug 出现后也能快速定位，不靠猜。

## 1) 默认背景提示词（建议长期开启，精简）

把下面这段粘贴到 Claude 默认背景提示词：

```text
你是我的全栈开发助手。默认采用“低 token 模式”：
1) 新功能优先最小改动，不主动加大量日志。
2) 只保证三件事：
   - 统一错误返回结构：{ success:false, errorCode, message }
   - 关键接口参数校验（缺字段要明确报错）
   - 不输出敏感信息（key/token/password）
3) 交付时只输出：
   - 最小复现步骤（3步）
   - 自检清单（正常/参数缺失/未登录）
4) 若我说“进入排查模式”，再补 requestId 与详细日志，不要提前铺满埋点。
```

---

## 2) 出 bug 再用：排查模式提示词（临时粘贴）

当线上/预览出现 bug，再把下面这段发给 Claude（不是默认常驻）：

```text
进入排查模式（仅本次任务生效）：
1) 不重构，先给最可能 3 个根因（按概率排序）。
2) 给 10 分钟内可执行的最小排查步骤。
3) 只在相关 API 增加临时埋点：
   - request_start（路由、用户、入参字段名）
   - before_external_call（URL、method、字段名）
   - after_external_call（status、耗时、返回摘要）
   - request_end（success/errorCode）
4) 返回体临时加 requestId，前端 console 打印 requestId。
5) 给我可复制的 curl 复现命令和 Vercel Logs 搜索关键词。
6) 问题修复后，删除临时高噪音日志，只保留必要错误日志。
```

---

## 3) Codex 能不能直接看 Vercel 运行情况？

简版结论：**通常不能“自动直接读取”你 Vercel 项目的后台日志**（除非你额外打通权限/API）。

所以实战做法是：
1. 你在 Vercel 页面复制日志文本给 Codex（最稳）。
2. 或者你把失败请求的关键信息（URL、状态码、响应体）贴给 Codex。
3. Codex 再帮你做根因排序、给最小补丁和下一步验证。

> 你可以理解为：Codex 是“诊断专家”，你是“日志搬运员”。

---

## 4) 傻瓜式找 bug（按你现在的习惯优化）

你现在已经会提供：页面/按钮/预期vs实际/控制台日志。很好。再加 2 个信息就足够高效：

- **Network 里该接口的 Request Payload / Response**
- **Vercel Functions Logs 里同时间段的报错行**

### 五步固定动作（3~10分钟）
1. 记录一句话：
   - 页面：
   - 点击了什么按钮：
   - 预期：
   - 实际：
2. 打开浏览器 DevTools → **Console**：复制红色报错。
3. 打开 DevTools → **Network**：点失败接口，复制：
   - Request URL
   - Request Payload
   - Response status + Response body
4. 打开 Vercel 预览部署 → **Functions / Logs**：复制同一分钟内日志（前后各30秒）。
5. 把 1~4 全贴给 Codex/Claude，并发这句：
   - “请先按概率给3个根因，不要重构；只给最小修复和回归步骤。”

---

## 5) 你可以复制的“报错信息打包模板”（发给 AI）

```text
【复现位置】/xxx 页面
【触发操作】点击“xxx”按钮
【预期结果】xxxx
【实际结果】xxxx

【Console 报错】
(粘贴)

【Network】
- URL:
- Method:
- Status:
- Request Payload:
- Response Body:

【Vercel Logs（同时间段）】
(粘贴)

请先输出：
1) 最可能 3 个根因（按概率）
2) 我先做哪一步验证（10分钟内）
3) 最小修复补丁（不要重构）
4) 回归验证步骤
```

---

## 6) 其他良好习惯程序员通常怎么做（实战版）

1. **先拿证据再改代码**：至少有 Console + Network + 服务端日志三类证据。
2. **分层定位**：前端组包 / 后端校验 / 第三方调用 / 数据层，逐层排除。
3. **小步改动**：日志补丁和业务修复分开提交，便于回滚。
4. **固定错误码**：同类错误始终返回同一个 `errorCode`。
5. **修后复盘**：补一条“如何复现、为何发生、如何避免”。

---

## 7) 预防 bug 的低成本方法（推荐先做这 5 个）

1. **接口契约检查**：新增/变更接口时写字段清单（必填、类型、示例）。
2. **部署前冒烟**：每次只测 3 条关键链路（登录/核心提交/核心查询）。
3. **环境变量清单**：预览与生产对照，改完后强制重部署。
4. **第三方防护**：统一超时、最多重试 1~2 次、报错可读化。
5. **回归模板**：每次修 bug 固定写“复现步骤 + 验证步骤”。

---

## 8) 一句话指令（直接发给 Claude/Codex）

```text
先低 token 处理：不要重构、不要铺满日志。基于我给的页面路径+操作+Console+Network+Vercel Logs，先给3个最可能根因和10分钟验证步骤；只给最小修复补丁与回归步骤。
```


---

## 9) 你提出的新流程：可行性评估（结论 + 缺点）

你的流程：
1. 先给 Claude/Codex 复现条件 + 控制台信息，让它们先读代码给“可能区域 + 手动缩小范围方法 + 还缺哪些证据”。
2. 你再按方法继续手动排查并回传证据。

### 可行性结论
**可行，而且是正确方向。** 这是“人机协作分层排查”模式：
- 你负责：复现与采集证据（事实层）
- Claude/Codex 负责：代码阅读与推理（分析层）

### 主要缺点（需要规避）
1. **只给 Console 容易误判**：Console 往往缺少请求体/响应体与服务端上下文。
2. **两边 AI 同时分析会分叉**：Claude 与 Codex 可能给不同方向，造成来回试错。
3. **缺少“停止条件”**：如果没有“排查到什么信息就能下结论”的标准，会无限循环。
4. **证据时间不对齐**：没记录发生时间和接口路径时，Vercel 日志难匹配。

### 优化建议（在你原流程上最小改动）
把“Console 信息”升级为“最小证据包 6 件套”：
1) 页面路径
2) 点击动作
3) 预期 vs 实际
4) Console 报错
5) Network（URL/Status/Request/Response）
6) Vercel 同时间段日志

并加 2 条规则：
- **单 AI 主诊断**：同一轮只让一个 AI 输出主结论，另一个只做交叉复核。
- **每轮必须产出下一步停止条件**：例如“拿到 response body 即可判断是否参数映射错误”。
